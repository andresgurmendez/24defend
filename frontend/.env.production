# Used by `npm run build` (Vite loads .env.production for production builds).
# In production there's no dev proxy, so this must point at the real API and
# the backend must allow this origin via CORS (see backend/app/config.py
# `dashboard_cors_origins`, which must include this dashboard's domain).
VITE_API_BASE_URL=https://api.24defend.com
